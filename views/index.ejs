<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ingresar al Sistema</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #e0f7fa;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .container {
            background-color: #ffffff;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.2);
            text-align: center;
            max-width: 400px;
            width: 100%;
        }
        h1 {
            color: #00796b;
            margin-bottom: 20px;
            font-size: 24px;
        }
        img {
            max-width: 100px;
            margin-bottom: 20px;
        }
        .menu {
            margin-top: 20px;
        }
        .menu form {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }
        .menu input {
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 16px;
        }
        .menu button {
            padding: 10px;
            background-color: #00796b;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
            font-size: 16px;
        }
        .menu button:hover {
            background-color: #004d40;
        }
        .menu a {
            color: #00796b;
            text-decoration: none;
            margin-top: 10px;
            display: inline-block;
            transition: color 0.3s;
        }
        .menu a:hover {
            color: #004d40;
        }
        .footer {
            position: absolute;
            bottom: 10px;
            left: 10px;
            font-size: 14px;
            color: #004d40;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Ingresar al Sistema</h1>
        <img src="/images/UVM.png" alt="Universidad">
        <div class="menu">
            <form method="post">
                <input type="text" placeholder="Usuario" name="user" required>
                <input type="password" placeholder="Contraseña" name="pass" required>
                <button type="submit">Ingresar</button>
            </form>
            <a href="/usuarios/Registro">¿No tienes cuenta? Regístrate</a>
        </div>
    </div>
    <div class="footer">
        Integrantes:<br>
        Br. Miguel Suarez. CI. V-28.206.469<br>
        Profesor: Yerson Gonzalez<br>
        Materia: Backend
    </div>
</body>
</html>
